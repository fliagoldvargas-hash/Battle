[package]
name = "token_battle_escrow"
version = "0.1.0"
description = "Token Battle SOL escrow for Devnet"
edition = "2021"
rust-version = "1.79"

[lib]
crate-type = ["cdylib", "lib"]
name = "token_battle_escrow"

[features]
default = []
no-entrypoint = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
